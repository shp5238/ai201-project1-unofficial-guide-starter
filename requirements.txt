sentence-transformers==3.4.1
chromadb>=0.6.0
groq==0.15.0
python-dotenv==1.0.1

# Query interface (Milestone 5) — custom chat UI served by Flask
flask>=3.0

# Document ingestion (Milestone 3)
pymupdf>=1.24.0         # extract text from local PDFs (robust glyph ordering)
transformers>=4.40.0    # all-MiniLM-L6-v2 tokenizer for token-accurate chunking
